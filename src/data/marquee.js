// Ticker band content. Kept as its own short list rather than derived from
// `skills`, because the marquee wants a punchy, recognisable subset — the names
// a recruiter scans for — not the full layered inventory.

export const marqueeItems = [
    'AWS Glue',
    'PySpark',
    'Databricks',
    'Delta Lake',
    'Oracle ODI',
    'Spark SQL',
    'Python',
    'PL/SQL',
    'Medallion architecture',
    'Data reconciliation',
];

export default marqueeItems;
